NEXT_PUBLIC_API_URL=/api
     # Optional if used by the dashboard/payments
     NEXT_PUBLIC_PAYMENT_LINK_URL=

     # NextAuth (admin area)
     # Generate a strong secret: `openssl rand -base64 32`
     NEXTAUTH_SECRET=change_me_in_local
     # Set to true only when troubleshooting NextAuth
     NEXTAUTH_DEBUG=false

     # PostgreSQL
    DATABASE_URL=postgresql://postgres:MqHSLcbilaKYNlGlBJvhhBHsqLcIYyFC@centerbeam.proxy.rlwy.net:45511/railway?sslmode=require
     # Note: postgres.railway.internal:5432 only works INSIDE Railway.
     # For local/Vercel use the PUBLIC proxy host above. Add `?sslmode=require` if needed.

     # JWT for app APIs (Next routes) — must be kept secret
     JWT_SECRET=dev_dev_dev_change_me

     # WhatsApp Bot service (Railway)
    BOT_SERVICE_URL=https://bot-01-production.up.railway.app
    BOT_INTERNAL_KEY=dev-secret-key
    COMPANY_WHATSAPP_NUMBER=919756862551

     # App URL for bot fallback Next.js API calls
     APP_URL=http://localhost:3000
     # Unique LocalAuth profile id for WhatsApp client (safe to change)
     BOT_CLIENT_ID=bot-dev
     BOT_PORT=4002
     # Optional: Default country code when phone missing CC
     DEFAULT_COUNTRY_CODE=91
     # Optional: server-side payment link if public one isn't set
     PAYMENT_LINK_URL=

     # Bot runtime (optional)
     # Set a custom Chrome path if not using the defaults for your OS
     # macOS example: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
     # Linux example: /usr/bin/google-chrome-stable
     # Windows example: C:/Program Files/Google/Chrome/Application/chrome.exe
     PUPPETEER_EXECUTABLE_PATH=
     # Run the bot in headless mode (no browser UI). Use true on servers.
     HEADLESS=false

     # Login flow controls
     # Enable non-WhatsApp QR (regular QR) endpoint. Defaults to disabled for strict WhatsApp-only flow.
     ENABLE_REGULAR_QR=false
