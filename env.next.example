# Next.js app environment example
# Copy to .env.local for local development or set on Vercel Project Settings

# Frontend -> Backend API base (use same-origin to avoid CORS)
NEXT_PUBLIC_API_URL=/api

# NextAuth (admin area)
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32 (or Vercel -> Secrets)
NEXTAUTH_SECRET=change_me_dev
NEXTAUTH_DEBUG=false

# PostgreSQL
# Use a CLOUD Postgres for both local and Vercel so data matches the bot
DATABASE_URL=postgresql://postgres:MqHSLcbilaKYNlGlBJvhhBHsqLcIYyFC@centerbeam.proxy.rlwy.net:45511/railway?sslmode=require
# Note: the internal host (postgres.railway.internal:5432) only works inside Railway.
# On Vercel or locally, use the PUBLIC proxy host above.
# If you see SSL errors, append: ?sslmode=require

# JWT secret for custom API auth (non-NextAuth flows)
JWT_SECRET=dev_dev_dev_change_me

# WhatsApp bot service (for notifications, OTP)
# For production: your Railway bot URL
BOT_SERVICE_URL=https://bot-01-production.up.railway.app
BOT_INTERNAL_KEY=dev-secret-key

# WhatsApp / phone normalization
COMPANY_WHATSAPP_NUMBER=919212079494
DEFAULT_COUNTRY_CODE=91

# Payment links (optional)
NEXT_PUBLIC_PAYMENT_LINK_URL=
PAYMENT_LINK_URL=

# Production (Vercel) overrides — set these in Vercel Project Settings
# NEXT_PUBLIC_API_URL=/api
# NEXTAUTH_URL=https://your-next-app.vercel.app
# NEXTAUTH_SECRET=<generated>
# DATABASE_URL=<your-cloud-db-url>
# BOT_SERVICE_URL=https://your-railway-bot.up.railway.app
# BOT_INTERNAL_KEY=<shared-secret>
